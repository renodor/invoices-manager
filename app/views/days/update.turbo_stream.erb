<%= turbo_stream.replace @day do %>
  <%= render @day, invoice: @invoice %>
<% end %>

<%= turbo_stream.update dom_id(@invoice, :calendar_total) do %>
  <%= render 'calendar_total', invoice: @invoice %>
<% end %>

<%= render_turbo_stream_flash_messages %>